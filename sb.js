function sortbyLength(arr) {
  let index = [];
  let sortedIndex = [];
  arr.forEach((a) => {
    index.push(a.length);
  });

  for (let i = 0; i < index.length; i++) {}

  return index;
}

const array = ['a', 'dog', 'of', 'food'];

console.log(sortbyLength(array));
